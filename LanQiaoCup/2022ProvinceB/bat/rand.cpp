#include <bits/stdc++.h>

using namespace std;

int main() {
  srand(time(0));
  random_shuffle(a.begin(), a.end());
  mt19937 rng((unsigned int) chrono::steady_clock::now().time_since_epoch().count());
  return 0;
}
